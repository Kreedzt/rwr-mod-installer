import React, { FC } from 'react';

type Step3Props = {
    setLoading: (next: boolean) => void;
};

const Step3: FC<Step3Props> = ({ setLoading }) => {
    return <div>Step3</div>;
};

export default Step3;
