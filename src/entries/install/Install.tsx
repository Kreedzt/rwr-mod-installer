import React, { FC } from 'react';

interface InstallProps {
    //
}

const Install: FC<InstallProps> = () => {
    return (
        <div>
            Install...
        </div>
    )
};

export default Install;
