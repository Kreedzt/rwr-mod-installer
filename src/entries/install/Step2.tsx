import React, { FC } from 'react';

type Step2Props = {
    setLoading: (next: boolean) => void;
};

const Step2: FC<Step2Props> = ({ setLoading }) => {
    return <div>Step1</div>;
};

export default Step2;
